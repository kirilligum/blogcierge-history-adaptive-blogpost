---
import Layout from "../layouts/Layout.astro";
---

<Layout title="BlogCierge -- history-addaptive AI blogpost">
	<main>
		<div class="hero-background">
			<div class="hero-container">
				<div class="flex">
					<div class="flex flex-col">
						<span class="blogcierge-symbol">⁂</span>
						<h1>
							Blogpost<br class="br-break-1" />
							adapted to your<br class="br-break-2" />
							knowledge
						</h1>
					</div>
				</div>
				<h2>
					We optimize the blogpost content for each user based on their reading
					history and questions to AI.
				</h2>
				<div class="hero-bullets">
					<ul>
						<li>Removes redundant information from the blogpost</li>
						<li>Explains new terms or confusing reasoning</li>
						<li>All based on your reading history and conversation with ai</li>
					</ul>
				</div>
				<div class="qr-code-container">
					<img
						src="/images/website-qr.png"
						alt="QR code for website"
						class="website-qr-code"
					/>
					<p>Scan to visit!</p>
				</div>
			</div>
		</div>

		<style>
			:root {
				/* Spacing */
				--spacing-lg: 2rem;
				--spacing-xl: 4rem;
				--spacing-2xl: 8rem;

				/* Sizes */
				--content-width: 800px;

				/* Typography */
				--text-3xl: 3em;
			}
			main {
				margin: 0 auto;
				background-color: var(--blgc-main-surface-primary);
				display: flex;
				flex-direction: column;
				align-items: center;
			}

			h1 {
				color: var(--blgc-text-primary);
				font-family: var(--font-heading);
				text-align: left;
			}

			h2 {
				font-family: var(--font-heading);
				/* text-align: right; */
			}

			/* Hero Section */
			.hero-background {
				background: linear-gradient(
					135deg,
					var(--hero-gradient-start) 0%,
					var(--blgc-main-surface-secondary) 100%
				);
				width: 100%;
				display: flex;
				justify-content: center;
				margin-bottom: 2rem;
				padding-bottom: 4rem;
				min-height: 100vh;
			}

			.mobile-only {
				display: none;
			}

			@media (max-width: 767px) {
				.hero-background {
					padding-bottom: 0;
				}
				.mobile-only {
					display: block;
				}
			}

			.hero-container {
				color: var(--hf-color);
				padding: 0rem 2rem;
				max-width: 1440px;
				width: 100%;
				box-sizing: border-box;
				h1,
				h2 {
					/* margin: 0.5rem 0; */
				}

				h1 {
					text-align: left;
					/* font-size: 4rem; */
					font-size: clamp(2rem, 10vw, 4rem);
					font-weight: var(--font-weight-bold);
					padding: 1rem 0;
				}

				h2 {
					padding: 1rem 0;
					/* font-size: 2em; */
					font-size: clamp(1rem, 5vw, 2rem);
					/* max-width: 768px; */
					/* margin-left: auto; */
					letter-spacing: 0.05em;
				}
			}

			.blogcierge-symbol {
				font-size: clamp(8rem, 40vw, 16rem);
				font-weight: bold;
				color: var(--hf-color);
				/* text-align: right; */
				display: block;
				/* margin-bottom: -0.1em; */
				/* margin-top: -0.2em; */
				/* padding-right: 48px; */
				padding-left: 2rem;
			}

			.knowledge-visual-container {
				display: flex;
				gap: 2rem;
				/* margin-bottom: 2rem; */
				margin: 4rem 0;
				padding: 1rem 0;
				overflow-x: auto;
				width: 100%;
				max-width: 100%;
			}

			.knowledge-visual-card {
				width: 320px;
				min-width: 320px;
				height: 480px;
				min-height: 480px;
				border: none;
				background-color: #fff;
				border-radius: 8px;
				overflow: hidden;
				flex: 0 0 auto;
			}

			.hero-bullets {
				/* width: 100%; */
				/* text-align: right; */
				ul {
					/* list-style: none; */
					/* padding: 0; */
					/* margin: 0; */
					/* width: 100%; */
				}

				li {
					/* text-align: right; */
					/* position: relative; */
					display: flex;
					/* justify-content: flex-end; */
					align-items: center;
					gap: 0.3em;
					font-size: 1.2em;
					margin-bottom: 0.5rem;
					padding: 0.2em 0;
				}

				li::before {
					content: "*";
					font-size: 2em;
					line-height: 0;
					/* order: 2; */
				}
			}

			.hero-contact {
				margin-top: 3rem;
				margin-bottom: 8rem;
				display: flex;
				justify-content: flex-start;
				padding-left: 2rem;
			}
			/* Hide our forced breaks by default */
			.br-break-1,
			.br-break-2 {
				display: none;
			}

			/* For viewports below 992px (i.e. max-width: 991px),
         show the first break so that "SEO for" is on its own line */
			@media (max-width: 991px) {
				.br-break-1 {
					display: inline;
				}
			}

			/* For viewports below 768px,
         also show the second break so that "Tomorrow's" and "LLMs" appear on separate lines */
			@media (max-width: 767px) {
				.br-break-2 {
					display: inline;
				}
			}

			.qr-code-container {
				display: flex;
				flex-direction: column;
				align-items: center; /* Centers the img and p elements */
				text-align: center; /* Ensures text within p is centered */
				margin-top: 3rem;
				margin-bottom: 2rem;
			}

			.website-qr-code {
				width: 600px; /* Doubled size */
				height: 600px; /* Doubled size */
				border: 5px solid var(--cstm-main-surface-secondary); /* Optional border */
				border-radius: 8px; /* Optional rounded corners */
				padding: 5px; /* Optional padding inside border */
				background-color: white; /* Ensure background is white if QR has transparency */
			}

			.qr-code-container p {
				margin-top: 0.5rem;
				font-size: 0.9em;
				color: var(--cstm-text-secondary);
			}
		</style>
	</main>
</Layout>
